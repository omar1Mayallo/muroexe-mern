import styled from "styled-components";

export const ItemsHead = styled.p`
  font-weight: 900;
  color: black;
`;
export const ListItem = styled.li`
  padding-bottom: 5px;
  transition: 0.5s;
  svg {
    transform: translateX(-4px);
  }
`;
